<VirtualHost *:80>
	ServerAdmin webmaster@localhost
  ServerName monprojet
	DocumentRoot /[chemin vers le projet]/monprojet/web
  <Directory "/[chemin versr le projet]/monprojet/web">
    AllowOverride All
    Require all granted
    Allow from All
  </Directory>
</VirtualHost>

# vim: syntax=apache ts=4 sw=4 sts=4 sr noet
